export enum UserMessages {
    USER_NOT_FOUND = "Specific User Not found",
    USER_DELETED = "User deleted successfully"
}

export enum GeneralErrors {
    UNAUTORIZED_ERROR = "Has no token in header, user not permitted",
    FORBIDDEN_ERROR= "User not permitted to perform this action"
}